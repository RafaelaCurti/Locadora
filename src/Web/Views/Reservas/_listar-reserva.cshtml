@model TReservation
<div class="col-lg-12 item-genero">
    <input type="hidden" name="Client" class="id-reserva" value="@Model.Id" />
    <p>
        <strong>@Model.Name</strong> - <a class="excluir-reserva" data-nome="@Model.Name" style="cursor: pointer">Excluir</a>
    </p>
</div>
